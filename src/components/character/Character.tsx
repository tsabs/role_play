import SafeView from '../library/SafeView';
import { Text } from 'react-native-paper';

const Character = () => {
    return (
        <SafeView title={'Character'}>
            <Text>Character</Text>
        </SafeView>
    );
};

export default Character;
